[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ocflindex"
version = "0.0.1"
dependencies = [
    "requests",
    "grpcio",
    "protobuf",
]

[tool.setuptools.packages.find]
where = ["ocflindex"]